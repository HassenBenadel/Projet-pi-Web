{% extends 'Bbase.html.twig' %}

{% block title %}Modifier la  Carte de Fidelite{% endblock %}

{% block body %}
  <div class="row">
        <div  style="display: flex;align-items: center;justify-content: center;margin-top: 150px; padding: 10px;">

            <div class="card">
                <div class="card-body">
    <h1>Regenerer une carte de fidelite: </h1>

                       <button class=class="btn btn-success">{{ button_label|default('Valider') }}</button>
                    </div>


           


    <a href="{{ path('app_carte_fidelite_index') }}">back to list</a>

    {{ include('carte_fidelite/_delete_form.html.twig') }}
   
     </div>
            </div>

        </div>

    </div>
{% endblock %}
