<form method="post" action="{{ path('app_code_reduction_delete', {'code': code_reduction.code}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ code_reduction.code) }}">
    <button class="btn">Delete</button>
</form>
