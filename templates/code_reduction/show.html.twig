{% extends 'base.html.twig' %}

{% block title %}CodeReduction{% endblock %}

{% block body %}
    <h1>CodeReduction</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Code</th>
                <td>{{ code_reduction.code }}</td>
            </tr>
            <tr>
                <th>Pourcentage</th>
                <td>{{ code_reduction.pourcentage }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_code_reduction_index') }}">back to list</a>

    <a href="{{ path('app_code_reduction_edit', {'code': code_reduction.code}) }}">edit</a>

    {{ include('code_reduction/_delete_form.html.twig') }}
{% endblock %}
