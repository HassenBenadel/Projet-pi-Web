{% extends 'base.html.twig' %}

{% block title %}Historique{% endblock %}

{% block body %}
<div class="row" style="display: flex;align-items: center;justify-content: center;margin-top: 150px;">

    <div class="col-lg-12">

        <div class="card">
            <div class="card-body">
    <h1>Historique de vos commandes </h1>

    <table class="table">
        <thead>
            <tr>
                <th>Reference </th>
                <th>Methode de paiement</th>
                <th>Total prix</th>
                <th>Total panier</th>
                <th>Date de la commande</th>
                <th>Code de reduction utilisé</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for commande in commandes %}
            <tr>
                <td>{{ commande.id_commande }}</td>
                <td>{{ commande.methpaiement }}</td>
                <td>{{ commande.totalprix }}</td>
                <td>{{ commande.totalpanier }}</td>
                <td>{{ commande.DateCommande ? commande.DateCommande|date('Y-m-d') : '' }}</td>
                <td>{{ commande.code }}</td>
                <td>
                   
                 {{ include('commande/_delete_form.html.twig') }}
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="8">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('app_commande_new') }}">Passez une commande?</a>
       </div>
        </div>

    </div>



</div>

{% endblock %}
